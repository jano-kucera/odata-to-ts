/**
 * Converts odata type into typescript type.
 * @param type Type to convert.
 * @param namespace Namespace of the types.
 * @returns Converted type.
 */
export declare function convertType(type: string, namespace: string): string;
